<!--pages/message/message.wxml-->
<view class="notice">
	<view wx:if="{{!noticeData}}" class="title">暂无消息</view>
	<view class="title">{{noticeData.title}}</view>
	<view class="content">{{noticeData.content}}</view>
<!--	<button class="read-btn" bindtap="readNotice">确认</button>-->
</view>
